# docker-compose.yml
version: '3.8'
services:
  credit-risk-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MLFLOW_TRACKING_URI=sqlite:///mlflow.db
    volumes:
      - ./mlflow.db:/app/mlflow.db
      - ./data:/app/data